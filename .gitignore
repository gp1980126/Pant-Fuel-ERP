# Dependencies
node_modules/

# Build output
dist/

# Environment & secrets — NEVER commit (README step 1)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Private per-station business data (Phase 13) — never tracked
src/core/private/

# Release archives — distribute via GitHub Releases, not git
*.zip
